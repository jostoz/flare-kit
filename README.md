# flare-kit

Full-stack $0/month Micro-SaaS boilerplate on the Cloudflare Free Tier. See `docs/TRD.md` for the full technical requirements document, including every free-tier limit verified against Cloudflare's current docs and the measurements backing each design decision. Adding a new route? See [`docs/adding-a-route.md`](docs/adding-a-route.md) first. **AI coding agent working in this repo? Read [`AGENTS.md`](AGENTS.md) first** — deploy path, secret-persistence behavior, migration rules, and testing gotchas that aren't obvious from the code alone.

## Stack

- **Router/SSR:** [Hono](https://hono.dev) + React 19 `renderToReadableStream` (direct, no build-pipeline dependency — see TRD §3.1)
- **Data:** Cloudflare D1 + Drizzle ORM (`drizzle/migrations/`)
- **Auth:** Better Auth, sessions in D1, PBKDF2-SHA256 via `crypto.subtle` (never bcrypt/argon2 — TRD §3.1.2)
- **Payments:** Stripe Checkout + idempotent webhooks
- **AI:** Workers AI by default, Gemini/DeepSeek as opt-in alternatives (`GOOGLE_AI_API_KEY`/`DEEPSEEK_API_KEY`) with smart complexity-based routing, vision, web search + MCP tool-calling, a daily Neuron-reconciliation cron, and a Telegram channel — per-user daily usage quota, sliding-window conversation memory in D1 (last 10 turns, not full history), provider-agnostic (`src/server/ai-providers.ts`)
- **Files:** R2 with client-direct presigned uploads
- **Rate limiting:** native `ratelimits` binding (not KV — TRD §0)

## Commands

```bash
bun install              # postinstall runs `wrangler types`
bun run dev              # wrangler dev, local
bun run typecheck
bun run typecheck:integration
bun test                 # vitest, Node environment: quota degradation + SSR CPU smoke test
bun run test:integration # vitest, real Workers runtime (Miniflare): D1 + auth + caching, see docs/adding-a-route.md
bun run budget:check     # TRD §7.2 data-quota projection gate
bun run size              # wrangler dry-run + gzip bundle size check (TRD §7.6)
bun run bench:ttfb        # TRD §7.4 multi-region TTFB against a live deployment
CLOUDFLARE_API_TOKEN=... bun run budget:cpu:live  # TRD §7.1 real cpuTime from a live deployment (see finding below)
CLOUDFLARE_API_TOKEN=... bun run deploy    # TRD §5 one-command provisioning
CLOUDFLARE_API_TOKEN=... bun run teardown  # tears down what bootstrap created
```

## Verified in this repo

- `bun run typecheck` / `bun run typecheck:integration` — clean.
- `bun test` — 4/4 passing (quota degradation contract, SSR CPU smoke test).
- `bun run test:integration` — 4/4 passing against the real Workers runtime (Miniflare: real D1, real `crypto.subtle`, real bindings) — sign-up → sign-in → authenticated `/dashboard`, an unauthenticated `/api/*` rate-limit check, and `/` cache headers. This suite is what would have caught 3 of the 4 bugs listed below before they ever reached a live deploy; see `docs/adding-a-route.md`.
- `bun run budget:check` — passes at 66.7% D1 read/write, 33.3% Workers requests, projected at 5,000 MAU.
- `wrangler deploy --dry-run` — builds clean with all 7 bindings; **600 KB gzip**, under the 1 MB budget.
- Live `bootstrap.ts` roundtrip against the real Cloudflare REST API — deployed to `https://flare-kit-app.jostoztado.workers.dev`.
- Full auth loop live: sign-up → sign-in → authenticated `GET /dashboard` (real D1 query: `users` + `ai_usage`) — HTTP 200 end-to-end with a real session cookie. This was **never exercised before**; fixing it surfaced four separate pre-existing bugs, all fixed and verified live (see below). Re-tested with 4 additional fresh sign-ups after the fixes — all 200, no flakiness.
- `GET /` edge-caching (TRD §7.4): `Cache-Control: public, max-age=60` + Workers Caching enabled (`cache_options` in the deploy metadata — the field name differs from `wrangler.jsonc`'s `cache` key; the raw multipart API doesn't read `wrangler.jsonc` at all). `Cf-Cache-Status` confirmed `MISS` → `HIT` on a second request.
- `bun run bench:ttfb` (TRD §7.4) — p75 131ms across 5 regions, warm cache (`Cf-Cache-Status: HIT`). The script checks `Cache-Control` dynamically now (it used to hardcode a claim that went stale the moment caching shipped) but asserts no pass/fail: this number is dominated by check-host.net's budget-VPS-to-edge network transit, not Cloudflare's server-side compute time, which is what TRD §7.4 actually budgets — a real regression there would need `Server-Timing` or a similar edge-side measurement, not an external network probe. `GET /dashboard` and `/api/*` are defended with `Cache-Control: private, no-store` (belt-and-suspenders against Cloudflare's 2-hour heuristic-freshness default once caching is enabled) — confirmed `Cf-Cache-Status: BYPASS`.
- Gemini AI provider live: `POST /api/ai/chat` with `GOOGLE_AI_API_KEY` set — HTTP 200 with a real generated response, `ai_usage` quota row written from Gemini's actual `usageMetadata.totalTokenCount` (not the character-count estimate used for Workers AI). Caught one real bug in the process: `gemini-2.5-flash` was retired for new accounts (404, "no longer available to new users") — updated to `gemini-3.6-flash` in `src/server/ai-providers.ts`.
- Workers AI provider live (temporarily redeployed without `GOOGLE_AI_API_KEY` to exercise the default path, then redeployed with it again — production runs Gemini): `POST /api/ai/chat` — HTTP 200, `ai_usage` quota row written. Both providers now confirmed working end-to-end, not just typechecked.
- Conversation memory live: two-turn exchange (`"My favorite color is teal"` → `"What is my favorite color?"`) — the model correctly answered "teal" on the second turn, proving `messages` (D1, `src/db/schema.ts`) round-trips through both providers' native chat formats (`ai-providers.ts`), not a stateless prompt each time. `budget:check` updated from a flat per-request D1-write assumption to a route-mix weighted model (`budget.config.ts`) after the naive flat model failed the gate at 100% D1 write quota — the flat model applied the chat route's 3-row write cost to every dynamic request, which no real traffic mix does.
- Smart routing live: `classifyComplexity()`/`resolveAiProvider()` (`src/server/ai-providers.ts`) route short/simple prompts to Workers AI and long/keyword-matched/deep-history prompts to Gemini when `GOOGLE_AI_API_KEY` is set. Verified against production with `wrangler tail --format json`: a simple prompt (`"Hi!"`) returned in-network (`wallTime` ≈ `cpuTime`); a complex prompt (multi-clause, "explain step-by-step... analyze...") showed `wallTime: 2743ms` vs `cpuTime: 31ms` — the gap is the outbound fetch to Gemini's API, confirming the routing decision. Caught one more real, unrelated production bug in the process: `@cf/meta/llama-3.1-8b-instruct` was deprecated by Cloudflare on 2026-05-30 (aliased internally to a now-dead model, `AiError` 5028) — updated `WORKERS_AI_MODEL` to `@cf/meta/llama-3.1-8b-instruct-fp8`, confirmed live.
- Vision live: `POST /api/ai/chat` accepts an optional `image: { mimeType, data }` (base64), validated (`image/png|jpeg|webp|gif`, max ~3MB) and forced through Gemini (`resolveAiProvider` in `src/server/ai-providers.ts`) since Workers AI's default model is text-only; missing `GOOGLE_AI_API_KEY` with an image throws `VisionUnavailableError` → HTTP 503. Images are never persisted to D1 — only this turn's request to the provider carries the bytes; the stored `messages` row gets a `[image attached]` text marker so history stays readable and small. Verified against production with a real 1x1 PNG: HTTP 200 with a vision-grounded response describing the image's color; invalid mime type correctly rejected with HTTP 400.
- Web search tool (function calling) live: `webSearchTool` (`src/server/tools.ts`, backed by DuckDuckGo's free keyless Instant Answer API) wired through a Gemini/DeepSeek function-calling loop (`GeminiProvider`/`DeepSeekProvider.generate` in `ai-providers.ts`, bounded to `MAX_TOOL_ROUNDS = 3`). Added `DeepSeekProvider` (OpenAI-compatible REST) as a cheaper-per-token, higher-rate-limit alternative to Gemini for the "complex" routing tier, preferred when `DEEPSEEK_API_KEY` is set, with automatic fallback to Gemini in `ai.ts` if DeepSeek's call fails (confirmed live: a real "Insufficient Balance" 402 from an unfunded DeepSeek account, followed by a real Gemini `429` from the fallback — both external, not code bugs; a session-time misdiagnosis blamed a "wiped secret" on `bootstrap.ts` replacing bindings on deploy, corrected after confirming via the Cloudflare API that **secret bindings omitted from a `bootstrap.ts` deploy are NOT removed** — they persist independently until explicitly overwritten or deleted). Fixed three more real, Gemini-3-specific bugs found live: (1) `gemini-3.6-flash` rejects `role: "function"` for tool results — Google's own docs still show it; the model needs `role: "user"` instead; (2) Gemini 3 requires the first `functionCall` part of a turn to echo back its `thoughtSignature` verbatim or the call 400s — fixed by threading the raw response part back instead of reconstructing `{name, args}`; (3) an exhausted tool-call loop returned empty text — now falls back to the last tool result. **Not independently live-verified end-to-end through `/api/ai/chat`**: blocked by Gemini's free-tier daily request quota being exhausted from this session's own extensive testing (confirmed via `wrangler tail`: `429`) and DeepSeek's unfunded test account (`402`) — both external/billing, not code issues.
- MCP (Model Context Protocol) support: minimal stateless client (`src/server/mcp.ts`) for a single remote server's `tools/list`/`tools/call` over the Streamable HTTP transport (JSON-RPC 2.0 over one POST per call, `text/event-stream`-framed responses even for a single reply). No `initialize` handshake or session tracking — confirmed live via raw `curl` against a real public server (`mcpplaygroundonline.com`) that both calls work as plain stateless request/response round trips. MCP tools are adapted into this repo's `ToolDefinition` shape and merged into the same Gemini/DeepSeek tool-calling loop as `web_search`. Caught a real compatibility bug live: Gemini's function-declaration `parameters` accept only a restricted OpenAPI-3.0-like subset of JSON Schema, not full JSON Schema — a real MCP tool's schema (`exclusiveMinimum`, `$schema`) 400'd outright; added `sanitizeGeminiSchema()` to strip unsupported keywords before sending. **End-to-end verification through `/api/ai/chat` blocked by the same Gemini quota exhaustion** as web search, above — the MCP client itself and the schema sanitizer are unit-tested (`test/mcp.test.ts`, `test/ai-providers.test.ts`) and the client is independently confirmed live via direct `curl` against the real public server.
- Scheduling (TRD §3.5 daily Neuron reconciliation) live: the pre-existing but previously unimplemented Cron Trigger (`triggers.crons: ["0 3 * * *"]` in `wrangler.jsonc`) is now wired to `reconcileNeuronUsage` (`src/server/cron.ts`) via a `scheduled` export in `src/index.tsx`. It sums the day's `ai_usage.neurons` estimate from D1, queries Cloudflare's real GraphQL Analytics API (`aiInferenceAdaptiveGroups`, account-scoped) for the actual request count and token totals, and upserts both into a new `neuron_calibration` table (`drizzle/migrations/0003_add_neuron_calibration.sql`) — visible drift instead of a hardcoded/trusted-forever estimate. Cloudflare doesn't expose any way to manually fire a deployed Worker's cron in production, and `wrangler dev --test-scheduled --remote`'s `/cdn-cgi/handler/scheduled` route is itself intercepted and blocked by Cloudflare's edge (`error 1042`) before reaching the dev Worker — so the full pipeline (D1 estimate → real GraphQL Analytics query → D1 upsert) was verified live by replicating `reconcileNeuronUsage`'s exact SQL/GraphQL calls against real production D1 and the real Analytics API (which returned genuine same-day usage data: 3 requests, 13/8 input/output tokens) and confirming the resulting row, then deleting the test rows. The `scheduled()` → `reconcileNeuronUsage()` dispatch itself, and graceful no-op when `CF_ANALYTICS_API_TOKEN` is unset, are covered by `test/integration/cron.test.ts` against the real Workers runtime (D1 real, GraphQL fetch injected via a `fetchImpl` parameter — this repo's `cloudflare:test` version has no in-isolate `fetchMock`, unlike `@cloudflare/vitest-pool-workers`, so a Node-side `vi.stubGlobal("fetch", ...)` can't reach code running in the workerd isolate these tests exercise).
- Channels — Telegram live: `POST /api/telegram/webhook` (`src/server/telegram.ts`) is wired to the exact same `runChatTurn` core (`src/server/ai.ts`, extracted from `/api/ai/chat` in this same change so both channels share conversation memory, quota accounting, routing, vision, and tool-calling with zero duplicated logic) under a synthetic `telegram:<chatId>` id — `messages`/`ai_usage` have no foreign-key constraint on `users`, so no shadow user row is needed. Auth is an `X-Telegram-Bot-Api-Secret-Token` header check (`TELEGRAM_WEBHOOK_SECRET`) since Telegram has no session cookie to check. Verified live end-to-end against production with real (if temporary/test) credentials: a missing/wrong secret header → HTTP 401; a correctly-authenticated update with a simple text message → HTTP 200, Workers AI generated "Hello", and both turns persisted to D1 under `user_id = "telegram:999888"` — confirmed via a direct D1 query, then cleaned up. The test `TELEGRAM_BOT_TOKEN`/`TELEGRAM_WEBHOOK_SECRET` were deleted afterward via the Cloudflare API's secret-delete endpoint (confirmed the route correctly reverts to `503 telegram_not_configured`) — set real ones to actually use this channel (see "Telegram setup" below).
- OpenRouter provider + resolved live verification: added `OpenRouterProvider` (`src/server/ai-providers.ts`, same `OpenAiCompatibleProvider` base class as `DeepSeekProvider` — refactored out to avoid duplicating the tool-calling loop) as the top-preference complex-tier provider (`OpenRouter > DeepSeek > Gemini`), opt-in via `OPENROUTER_API_KEY`, with `ai.ts`'s fallback now trying every remaining configured provider in that order rather than a single hardcoded DeepSeek→Gemini pair. Model is `openai/gpt-4o-mini` — a real, live compatibility bug ruled out the cheaper first choice: `meta-llama/llama-3.3-70b-instruct` (via OpenRouter's DeepInfra upstream) doesn't reliably emit OpenAI-shaped `tool_calls`, instead returning a literal `<function\web_search{...}</function>` text string this repo's tool-calling loop (which looks for `message.tool_calls`) never detects — confirmed live, `gpt-4o-mini` returns proper `finish_reason: "tool_calls"`. This unblocked the previously-blocked end-to-end verification: web search and MCP were confirmed live against production through the real `/api/ai/chat` route (not just their standalone pieces) — a "current population" query showed a real multi-round tool-calling exchange (`wallTime: 2487ms` vs `cpuTime: 16ms`, 2 OpenRouter calls + 1 DuckDuckGo call) with a coherent final answer, and an `analyze_data`-targeted prompt correctly invoked the real public MCP server's tool and summarized its (mock) sales data in the final reply. Also corrected an earlier misreading: an OpenRouter response occasionally reused across two consecutive test requests initially looked like a stale deploy, but was actually the *same* malformed-tool-call model output recurring — not a caching bug.
- Landing page (`src/app/components/Landing.tsx`) + a real, previously-undiscovered production bug: a hero, 6 AI-feature cards, and a progressive-enhancement sign-up/sign-in form (`public/app.client.js`, posts JSON to the existing better-auth endpoints — inert without JS, no server-rendered fallback action, since this is a boilerplate demo not a production auth UI) replaced the placeholder table `Dashboard.tsx` rendered. Verifying it live surfaced a real bug that had shipped silently all session: **`/app.css` and `/app.client.js` 404'd in production** — `bootstrap.ts`'s raw multipart deploy path never uploaded `public/`'s static assets at all (`wrangler.jsonc`'s `assets` block is Wrangler-only config this raw API doesn't read, same caveat as `cache_options`). Fixed by implementing Cloudflare's Workers Static Assets direct-upload flow by hand (`src/lib/cf/client.ts`: `createAssetsUploadSession` + `uploadAssetBucket`, wired into `bootstrap.ts`'s `uploadStaticAssets` — manifest with Cloudflare's own hash algorithm, per-bucket upload, completion JWT passed to the script deploy's `metadata.assets`). Confirmed live: `GET /app.css` and `/app.client.js` now both return `HTTP 200` with byte-identical content to the source files and correct `Content-Type` (`text/css`, `text/javascript` — not `application/null`, which would have broken the stylesheet and the module script). The sign-up form was verified against the real `/api/auth/sign-up/email` endpoint it targets. **No visual/screenshot confirmation was possible**: the browser automation tool's Chromium relay failed to connect in this environment (`browser relay... extension never connected`) — verification here is structural/content-based (HTML markup, CSS/JS byte-match, HTTP status/headers), not a rendered screenshot.

## Telegram setup

`POST /api/telegram/webhook` (`src/server/telegram.ts`) shares the exact same conversation-memory/quota/routing/tool-calling core as `/api/ai/chat` — see "Channels — Telegram live" above.

1. Message [@BotFather](https://t.me/BotFather) on Telegram, `/newbot`, follow the prompts — this gives you a bot token (`TELEGRAM_BOT_TOKEN`).
2. Generate a random secret for `TELEGRAM_WEBHOOK_SECRET` (e.g. `openssl rand -hex 32`) — this is never sent to Telegram as a credential, only registered as the `secret_token` Telegram must echo back on every webhook call.
3. Deploy with both set: `CLOUDFLARE_API_TOKEN=... TELEGRAM_BOT_TOKEN=... TELEGRAM_WEBHOOK_SECRET=... bun run scripts/bootstrap.ts`.
4. Register the webhook with Telegram (one-time, until you rotate the URL or secret):
   ```bash
   curl -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/setWebhook" \
     -H "Content-Type: application/json" \
     -d "{\"url\":\"https://<your-worker>.workers.dev/api/telegram/webhook\",\"secret_token\":\"$TELEGRAM_WEBHOOK_SECRET\"}"
   ```
5. Message your bot on Telegram. Text and photos (with or without a caption) both work — a photo routes through the same vision path as `/api/ai/chat`'s `image` field, and forces Gemini the same way (`VisionUnavailableError`'s message is sent back as the bot's reply if `GOOGLE_AI_API_KEY` isn't set).

Rotate `TELEGRAM_WEBHOOK_SECRET` by generating a new one, redeploying, and re-running `setWebhook` with the new value — an old secret stops working the moment the new one is deployed (Cloudflare secrets persist across `bootstrap.ts` deploys that omit them; only an explicit new value or the Cloudflare API's secret-delete endpoint changes them).

## Bugs found and fixed while wiring the first real authenticated route

All four were live, pre-existing, and silent — nothing exercised `/dashboard` or any `/api/*` route end-to-end before this session, so none had ever thrown:

1. **`src/lib/cf/client.ts`** — `request()` hardcoded `Content-Type: application/json` on every call, stomping the multipart boundary `fetch` generates for `FormData` bodies. Broke `deployWorker`.
2. **`src/db/schema.ts` / `src/server/auth.ts`** — better-auth requires `account` and `verification` tables (credential passwords live in `accounts.password`, not a `users` column) plus `emailVerified`/`image`/`updatedAt` on `users` and `token`/`createdAt`/`ipAddress`/`userAgent` on `sessions`. None of this existed; every authenticated request threw `Drizzle schema mismatch`. Fixed with `drizzle/migrations/0001_add_auth_tables.sql` (production `users` had 0 rows at the time — no backfill needed) and explicit `{ mode: "timestamp_ms" }` on every better-auth-owned date column (without it, Drizzle passes raw JS `Date` objects to D1 instead of epoch millis).
3. **`src/server/auth.ts`** — `PBKDF2_ITERATIONS = 210_000` exceeds the Cloudflare Workers runtime's hard cap of 100,000 iterations (`NotSupportedError`, production-only — `wrangler dev`/Node both allow more, which is why this was never caught locally). Capped at 100,000.
4. **`scripts/bootstrap.ts`** — the deploy metadata's `bindings` array never included the `ratelimit` binding (`API_LIMITER`) or an `AUTH_SECRET` secret, both present in `wrangler.jsonc` but silently absent from every deploy done through `bootstrap.ts` instead of `wrangler deploy`. `API_LIMITER` being `undefined` crashed every `/api/*` request before even reaching a route handler. `AUTH_SECRET` is now generated once and persisted in the gitignored `.flare-kit.state.json` so reruns reuse it instead of rotating it and invalidating every live session.

## TRD §7.1 CPU gate — measured live, currently failing as literally specified

`bun run budget:cpu:live` (new: `scripts/budget-cpu-live.ts`) reads real `cpuTime` from `wrangler tail --format json` against the live deployment — the authoritative measurement TRD §7.1 calls for, distinct from `test/budget.cpu.test.tsx`'s local Node-environment smoke test. Run twice independently, 40 sequential (not concurrent) cache-busted requests each:

| Run | p50 | p90 | p99 | ≤7ms budget |
| --- | --- | --- | --- | --- |
| Sequential, 300ms spacing | 1ms | 11ms | 17ms | 90% |
| Sequential, 300ms spacing | 2ms | 11ms | 26ms | 88% |

**p99 exceeds the 7ms budget in every run.** p50 (1-2ms) closely matches the local SSR benchmark (TRD §3.1.1: 1.57ms p50) — the warm/steady-state render path is not the problem. The p90-p99 tail is Workers isolate cold-start / module-init cost, which TRD §7.1 explicitly includes in this budget by design ("incluye el primer request del isolate") but which no change to `src/` can eliminate — it's a platform characteristic of the isolate model, not an application regression. A genuine dependency or code regression would show up as an elevated **p50**, not just an elevated cold-start tail; that's still what `budget:cpu:live` is useful for catching, even though the literal p99 ≤ 7ms gate as written doesn't pass under real traffic.

## Deviation from the original plan

TanStack Start's own Vite/Nitro build pipeline for the Cloudflare Workers target was not wired in — it can't be verified end-to-end without a live deploy, and shipping an unverified build pipeline is worse than being explicit. The default SSR here is architecturally equivalent (React SSR on the Worker, TanStack Router for client navigation) and is fully measured: see TRD §3.1.1.
